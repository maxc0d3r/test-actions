name: Sync all branches with master branch in same repository
on:
  push:
    branches:
      - master
jobs:
  repo-sync:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
      with:
        fetch-depth: 2147483647
    - run: scripts/sync_all_branches_with_master.sh

